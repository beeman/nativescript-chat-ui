import { Routes } from '@angular/router';

import { ContactsListComponent } from './containers/contacts-list.component';

export const routes: Routes = [{ path: '', component: ContactsListComponent }];
